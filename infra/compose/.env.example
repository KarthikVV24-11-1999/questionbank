# Copy to .env before `docker compose up`. Every value is a placeholder —
# F39 (secret-rules.spec.ts, M0-23) scans this file for anything real-shaped.
AUTH_SIGNING_KEY=replace-with-a-real-32-byte-random-value
