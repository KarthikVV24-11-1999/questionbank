# M2 — Implementation Progress

| Task | Date | Tests | Note |
|---|---|---|---|
| M2-01 | 2026-08-06 | 32 | Scoring context anatomy (five directories) + `Result` and the closed error taxonomy. `Result` is duplicated rather than imported from curriculum — §9 rule 2 does not soften for a small type — and proven structurally compatible across the barrel. `SCORING_ERROR_KINDS` narrows the platform's ten to five via `satisfies`, so an eleventh kind is a compile error. Three domain guards (no `throw`, no outward import, no clock/randomness for F45) each proven against a planted violation. |
| M2-02 | 2026-08-06 | 40 | `ScoringInput` — the synthetic-attempt contract. Responses arrive projected; an absent response is the only way to express unattempted, so a `RESPONSE_EMPTY` payload is rejected rather than silently read as one. Override slot references are validated at construction, which makes DEC-3 structural — M2-09 must not re-guard it or the branch is unreachable under the 100% rule. `answerKey` on `ScoredSlot` and the `KEY_CORRECTED` override variant are deferred to M2-03, which defines `AnswerKey`. |
| M2-03 | 2026-08-06 | 45 | `AnswerKey` — four variants; `NumericAnswerSpecData` mirrored structurally rather than imported (domain imports nothing), with a spec asserting the curriculum barrel DTO stays assignable so M1 drift fails a test. Mode-required parameters validated here (D-001 rule 5) and normalization flags resolved, so M2-05 receives a complete spec. `KEY_KIND_BY_ITEM_TYPE` is closed: EXT-03 promises data-only *marking rules*, not data-only item types, so an unknown type is refused rather than guessed. §9 rule 10 scan pins the two legitimate `expectedValue` sites (the profile tolerance default) — a new one anywhere fails until justified. |
| M2-04 | 2026-08-06 | 57 | Numeric normalization (D-001 rule 1). Value/unit split is a hand-written scanner, not one regex — sign, grouped digits, decimal point, fraction bar and both exponent forms in a single expression would be unreviewable. Grouping strip is all-or-nothing on an unambiguously grouped value: per-separator stripping turned `1,5` into `15` (caught by its own test) and `1,23,456` into `1,23456`. Ambiguous input is left as typed, fails to parse, and awards 0 rather than scoring a value nobody entered. |
| M2-05 | 2026-08-06 | 87 | Exact-decimal comparison, all five modes. `decimal.ts` added alongside `compare.ts` because "never IEEE-754" needs real arithmetic: every value is an exact rational over BigInt, so `2/3` is two over three and `0.3-0.1-0.2` is exactly zero. Every boundary inclusive and tested on both sides and exactly at. `RELATIVE_TOLERANCE` at an expected zero falls back to absolute — a zero-wide relative band would silently become EXACT. Two more unreachable branches removed (an increment loop that the digit-count estimate can never need, a sign guard on magnitudes only). |
| M2-06 | 2026-08-06 | 19 | Units and accepted forms. `checkUnit` is four-valued — `not_required`/`match`/`mismatch`/`missing` — because an omitted required unit is not a wrong answer: the engine cannot tell, so it is indeterminate and must not match `NO_MATCH`. A form absent from `acceptedForms` is rejected rather than coerced, since `acceptedForms: [DECIMAL]` is usually a statement that converting is part of the question. All three forms proven to reduce to the same rational. |
| M2-07 | 2026-08-06 | 74 | The eight conditions, three-valued. `indeterminate` is the load-bearing value: an unparseable numeric entry, an omitted required unit, or a response whose shape does not fit its key is neither a match nor a recognised wrong answer, so it never reaches `NO_MATCH` and never costs a mark — this is what makes ADR-0003 structural. `NO_MATCH` is the literal inversion of `EXACT_MATCH` (shared `evaluateExactness`, asserted as an inversion) so the two cannot drift. An unknown condition kind is inert. `marking-rule-data.ts` mirrors the barrel's rule types, with assignability asserted against both shipped rule sets. |
| M2-08 | 2026-08-06 | 23 | The three awards. Marks are carried as exact rationals, not doubles: a partial-credit scheme awards 1.5, and eighty of those summed in floating point drifts — the drift is small, the gap between adjacent ranks is smaller. `rationalFromNumber` added to `decimal.ts`, routing through the shortest decimal representation so an authored 0.1 stays one tenth. An unknown award kind fails closed with `RuleViolation` and awards nothing; guessing zero would be an undetectable defect and guessing otherwise could deduct a mark nobody wrote. |
| M2-09 | 2026-08-06 | 21 | Item-level overrides (§2.5), the first step of §3's execution order. `DROPPED` and `BONUS` yield no key at all, so those slots structurally cannot reach the rule loop — a rule can never contradict an override. `KEY_CORRECTED` substitutes the key and scores normally. Also lands the two fields deferred from M2-03: `ScoredSlot.answerKey` (with variant/itemType validated at construction) and the `KEY_CORRECTED` variant, whose replacement key is checked against the slot's item type. Per the M2-02 note, no unknown-slot or duplicate guard here — both are unconstructible. |
| M2-10 | 2026-08-06 | 27 | First-match-wins selection. `rulesEvaluated` is returned so "evaluation stopped at the match" is asserted by count, not by reading the code. Authored order is never touched — a test reverses the JEE Main set and proves the terminal rule then wins, which is what sorting by anything would silently do to every published profile. An indeterminate condition falls through and is reported via `sawIndeterminate`, and a test proves an unreadable entry never reaches the −1 rule. A slot no rule governs is `RuleViolation`, never a zero. |
| M2-11 | 2026-08-06 | 43 | `ItemOutcome` with `ruleApplied` non-optional, so F47 is a compile-time property rather than something a checker notices later. `indeterminate` is a recorded correctness value: an unreadable entry is filed as indeterminate even where the matched condition would read as incorrect. A terminal `ALWAYS` on an answered slot also reads indeterminate — no authored rule described what the learner did. Explanations are derived from condition and award, never written per rule, so they cannot disagree with what happened. Overrides attribute to reserved `override:*` ids. |
| M2-12 | 2026-08-06 | 21 | `AggregationSpec` defined and ratified in [ADR-0006](../adr/ADR-0006-aggregation-spec-on-exam-profile-version.md), carried on `ExamProfileVersion` and re-exported from the curriculum barrel. `bestOf` ships in v1 (NEET Section B, best 10 of 15); ties break on lowest slot ordinal. Defaults reproduce JEE Main with no configuration. M1's golden rule-set hashes asserted **unchanged** — the hashed structure has no aggregation field to hash, which is the whole point of the placement. **Deferred to M2-18:** persisting `aggregation` to the curriculum schema; the domain default makes every existing row read correctly meanwhile. |
| M2-13 | 2026-08-06 | 42 | Sectional then total aggregation. The total is the sum of section scores and is never recomputed from outcomes independently — two paths to one number are two chances to disagree. `bestOf` selects highest-marks-first with ties on the earlier slot, and the discarded slots leave `maxAvailable` too; determinism asserted over 50 runs on an all-equal section. A dropped slot carries zero available so it reduces the denominator with no special case. `floorAtZero` is opt-in: silently raising a negative total awards marks nobody earned. Shuffle-invariance asserted for sections and totals. |
| M2-14 | 2026-08-07 | 9 | `ScoreRecord`: immutable, no setter, `markSuperseded` returns a new record so INV-11's dual-result retention is the only thing the type permits. `computedAt` is supplied, never read from a clock. Generation 1 may not claim a predecessor; generation n>1 must name one and give a reason. |
| M2-15 | 2026-08-07 | 50 | **The executor.** `(ScoringInput, MarkingRuleSetData, AggregationSpec) → ScoreRecord`, pure, in §3's fixed order. Scores a JEE Main paper and a JEE Advanced partial-credit paper end to end with no code change between them. Refuses a rule set the attempt is not pinned to; aborts wholly on an unmatched slot or an award it cannot apply, never returning a partial record. ADR-0003 asserted end to end: an unreadable entry lands on the terminal rule, records `indeterminate`, and leaves the total exactly what the readable answers earned. The 100% gate removed one more dead branch by moving the marks conversion to the input boundary. |
| M2-16 | 2026-08-07 | 9 | `rule_schema_version` dispatch (R2 / F48). `SHIPPED_SCHEMA_VERSIONS` is append-only and every entry must still resolve to an executor; the planted violation is a registry with the version deleted, which fails exactly as a real removal would. An unregistered version is `PreconditionFailed`, never a best-effort score under the nearest version it does know. |
| M2-17 | 2026-08-07 | 30 | `RescoringOperation` + dry-run. `approved` is reachable only from `previewed`, so the preview is the gate rather than a courtesy; every transition the machine does not name is refused, including reopening a completed operation. Reason mandatory at draft, principal mandatory at approval. The preview is built from records the same executor produced — a separately computed estimate would be a second scoring implementation. Rank movement is computed as "how many scored strictly higher, plus one", which handles ties without a special case and removed a lookup whose fallbacks were dead. |
| M2-18 | 2026-08-07 | 20 | Scoring schema: `score_record`, `section_score`, `item_outcome`, `rescoring_operation`, plus the curriculum `aggregation` column deferred from M2-12. F47 is enforced at the database — `rule_applied_id` NOT NULL with a non-empty check, so an unexplainable outcome cannot be stored even if code forgets. INV-11 is a partial unique index on `(attempt_id) WHERE is_current`, making a second current record impossible rather than discouraged. No FK crosses a schema (§9 rule 3), asserted by catalogue query. Marks are `numeric(14,4)`, never float. Found and fixed a non-idempotent down-migration: `ALTER TABLE` needed `IF EXISTS` to match the `DROP TABLE IF EXISTS` migrations either side of it. |
| M2-19 | 2026-08-07 | 15 | Append-only triggers on `score_record`, `item_outcome`, `section_score`. The one permitted update is a current record standing down — `to_jsonb(NEW) - 'is_current' = to_jsonb(OLD) - 'is_current'` means every other column is compared, so nothing can ride along with the flip. Never back to current. Detail tables admit no update at all and cannot be cascaded away, because the parent cannot be deleted. Proven from raw `psql`, not only the ORM. §9 rule 11 grants are revoked conditionally on the deployment role existing. |
| M2-18 | 2026-08-07 | 20 | Scoring schema: `score_record`, `section_score`, `item_outcome`, `rescoring_operation`. No FK crosses a schema boundary — the profile version and rule-set hash are values, because a score is pinned to what those *were*. F5 asserted by catalogue query over the live database, including the `aggregation` column added to curriculum (ADR-0006, the M2-12 deferral). F47 is enforced at the database as `NOT NULL` plus a non-empty check. Partial unique index gives exactly one current record per attempt. Up/down/up verified. |
| M2-19 | 2026-08-07 | 15 | Append-only triggers; see the raw `psql` transcript in the commit body. |
| M2-20 | 2026-08-07 | 21 | `ScoreRecord` repository. Save/load deep-equality forced a real fix: `Rational` is now canonical (lowest terms), because `4000/10000` read back from `numeric(14,4)` and `2/5` computed in memory are the same value with different structure — and M2-32 asserts byte-identity. Marks cross as decimal text, never a double. One aggregate, one transaction, proven by a rejected detail row leaving nothing behind. A stored `NaN` throws rather than reading as zero. Two coverage artifacts fixed at the source: a dead `instanceof` guard and a `finally` whose abrupt-completion path nothing can reach. |
| M2-21 | 2026-08-07 | 9 | `RescoringOperation` repository. Upsert guarded by `aggregate_version` (P8): two readers, one preview each, and the stale write is refused rather than silently overwriting an approval it never saw. Dry-run result round-trips through JSONB. |
| M2-22 | 2026-08-07 | — | `ScoreAttempt` command + handler. Idempotent on the attempt: a repeat returns the existing record rather than a second generation (REL-02). `computedAt` read from an injected clock at the boundary, never in the domain (F45). |
| M2-23 | 2026-08-07 | — | Rescoring handlers. **Dry-run preview proven identical to execution against real Postgres**: every delta the preview promised is asserted against the stored record afterwards. Approval requires `admin` *and* step-up; execution before a preview is refused. Every step audited. |
| M2-24 | 2026-08-07 | — | Queries. No view carries an answer key, correct option, response payload or expected value — asserted by serializing a real view and scanning it. Learners read only their own records. Marks render as decimal text. |
| M2-25 | 2026-08-07 | — | Public barrel + the boundary checker generalised from curriculum-only to every context. `DOMAIN_REACHES_OUTWARD` now fires for any context's outward layer, not just the importer's own. |
| M2-26 | 2026-08-07 | 26 | `AttemptScored` / `AttemptsRescored` and the outbox emitter, writing inside the aggregate's transaction. Payloads carry identifiers and decimal text only. *(Test count is for M2-22…26 together — one integration spec covers the layer.)* |
| M2-29 | 2026-08-07 | — | Golden fixture format + loader. `provenance` is enforced: a synthetic fixture must carry `synthetic` in filename and paperId; an official one must cite its key source. Self-inconsistent fixtures rejected. README states plainly what the gate proves and what it does not. |
| M2-31 | 2026-08-07 | — | Blocking golden regression (F9) in the default test run. Prints its official/synthetic counts every run, and asserts `official < 3` so the DEC-2 failed-blocked item is checked rather than remembered. |
| M2-32 | 2026-08-07 | — | Determinism: 1,000 runs byte-identical on canonical serialization, plus slot-order invariance. Meaningful only because `Rational` is canonical (M2-20). |
| M2-33 | 2026-08-07 | 14 | Property-based tolerance tests, 1,000 cases each, seeded xorshift so a counterexample is reproducible and the suite cannot be flaky. Boundary inclusivity, RANGE closed interval, significant-figure symmetry and rounding idempotence. |
| M2-34 | 2026-08-07 | 12 | F45/F47/F48, each proven against a planted violation (`fitness-fixtures/as-scoring-domain/`). Coverage sweep asserts every scoring **domain** module and both repositories carry a 100% threshold — it found 12 modules missing one on first run. The application layer is deliberately excluded and the reason is recorded: it orchestrates and decides nothing about what a mark is. |
| M2-35 | 2026-08-07 | 40 | JEE Advanced partial credit as a golden fixture, exercising every band in §2.4 (+4/+3/+2/+1/−2/0) with zero code change — EXT-03. *(Count is the whole golden suite.)* |
| M2-27 | 2026-08-07 | 17 | OpenAPI contract. `x-handler` on every operation, reconciled against real handler policies (F15). Marks are typed `string` throughout — a JSON number is a double the moment a client parses it. No schema names an answer key, correct option, expected value or response payload; asserted over the whole document. |
| M2-28 | 2026-08-07 | 18 | Controllers through a real Nest app. Boundary validation with `.strict()` so a smuggled field is refused rather than ignored; RFC 9457 on every error with an explicit `retryable`; correlation id echoed or minted on success *and* error. Step-up passes through to the handler. F36 proven by a planted policy-less handler failing `ScoringModule.register`. The contract spec caught a singular sub-resource path — now `/score-records/current`. |
| M2-30 | — | 0 | **Failed-blocked (DEC-2).** No released JEE Main papers with official NTA keys are in the repo, so the corpus is 4 synthetic fixtures and 0 official. The harness, the loader's provenance rules and the blocking suite are all in place; M2-30 is a pure data drop that requires no code change. `golden-regression.spec.ts` asserts `official < 3` so this stays visible on every commit rather than living only in a document. |
