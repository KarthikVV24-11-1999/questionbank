# M0 — Progress Log

One sentence per task, appended as it lands.

- **M0-01** — Workspace glob coverage and required-script gates added (`workspace-rules.ts`), the offline install verified stable before and after, `packages/contracts` and `packages/domain-types` given real `test`/`typecheck` infrastructure they were missing, and ADR-0011 records the Turborepo deferral.
- **M0-02** — `loadConfig`/`loadConfigFromProcessEnv` added as the one typed, total config module with no unsafe defaults, and F16's enumerated-allowlist gate proven against a planted `process.env` reader, both at 100% coverage.
- **M0-03** — The allowlist serializer and structured JSON logger land, recursive dropping proven against a nested-PII fixture and level filtering including production's debug-off rule, both at 100% coverage.
- **M0-04** — The `Telemetry` port lands with `createJsonTelemetry` and the `RecordingTelemetry` double, parent linkage tracked via `AsyncLocalStorage` across awaits, spans filtered through M0-03's same allowlist, and a spec asserting no `@opentelemetry` import exists anywhere — all at 100% coverage.
- **M0-05** — The auth stub (`issue`/`verify`) lands over `node:crypto` HMAC-SHA256 with no alg-confusion shortcut and length-checked constant-time comparison; `verify` returns exactly a `PrincipalRef`, `ai_agent` verifies like `human` (D10), and ADR-0014 records what the stub deliberately is not — no user store, password, refresh, revocation or role assignment, all M8's. 100% coverage.
- **M0-06** — `createPrincipalResolver` lands as the one adapter shared by all three contexts' `PrincipalResolver` declarations, every refusal returning `null` and logging an identically-shaped record whose `errorCode` alone carries the distinguishable reason, with the raw token proven absent from every log entry. 100% coverage.
- **M0-07** — `SystemClock`, `UuidIdentifierFactory`, `createPool`/`closePool` and `PostgresAuditRecorder` land; the new `platform.audit_record` table (migration `20260813120000`) serves all three contexts' independently-declared audit ports from one physical table, append-only by trigger, proven against real Postgres including a rejected UPDATE and DELETE. 100% coverage on the correctness-bearing pieces; integration specs green against the local cluster.
- **M0-08** — `PostgresIdempotencyStore` **closes D22**: durable across a fresh instance against the same database, `remember()` is `INSERT ... ON CONFLICT DO NOTHING` so two genuinely concurrent inserts for one key resolve to exactly one row (proven with `Promise.all` racing two connections, not a read-then-write), expiry honoured on read and reaped on request. A type-level assertion and a shared contract spec prove it satisfies content's real `IdempotencyStore` port and behaves identically to `InMemoryIdempotencyStore`, which stays untouched. 100% coverage.
- **M0-09** — Blocked mid-session on two real defects the first typecheck surfaced: `MediaBlock.caption` diverged (`string` on the renderer, `Inline[]` on the domain — fixed, domain wins, seam spec now checks shape not just kind names) and the task's own premise that no JSX transform was needed was false (`renderFor` calls a `.tsx` component; fixed with `jsx: "react-jsx"` in `apps/api/tsconfig.json`, narrowed by `checkNoTsxFiles`, recorded in ADR-0016). `RenderValidatorAdapter` **closes D27**: the publication precondition now runs against the real renderer, proven in an integration spec where unrenderable LaTeX blocks publication via FR-QM-14 rule 2 against real Postgres. Task entry corrected in place. 100% coverage.
- **M0-10** — `FilesystemMediaStore` implements content's `MediaStore` port unchanged (`put`/`head`, no `delete` — the port has none), content-addressed by sha256 with a sharded S3-prefix-style layout; `createMediaStore` is the one place `nodeEnv` and adapter selection meet, throwing `ProductionMediaStoreRefusedError` in production rather than silently selecting local disk — `createApplication` (M0-12) will let that propagate as a boot failure. Path-traversal storage keys rejected before any filesystem call, eight shapes proven. 100% coverage.
- **M0-11 (complete)** — Fixed the M2 defect blocking scoring's composition first: `ScoreRecord` now carries `examProfileVersionId`/`taxonomyVersionId` (ADR-0017), confirmed the DB columns already existed and the old round-trip test was blind to both fields exactly like the renderer-seam caption; corrected M2-TRACEABILITY.md (finding F-7) and M2-CLOSEOUT.md in place. All three contexts now have `public/composition.ts` — content (37 handlers), curriculum (22), scoring (8) — each proven to resolve every handler its own OpenAPI document names via a real NestJS testing module, each refusing at runtime on a missing dependency. `PostgresEventPublisher` added for scoring's event ports, one transaction per publish, its non-atomicity with the aggregate's own save documented rather than hidden. ADR-0015 records the composition-seam pattern. Golden set unchanged: 40 pass, 0 official, 4 synthetic.
- **M0-12** — `createApplication` (`platform/composition/app-factory.ts`) wires the three contexts' `register()` and every platform adapter into one `INestApplication` via a `RootModule.register(imports)` following the existing static-register convention; F1 is extended to `platform/` (proven against a planted deep import of `contexts/content/infrastructure/item.repository.js`, and the real tree confirmed non-vacuously scanned); F11 is proven across the composed handler set via a planted policy-less handler substituted through `overrides.contentRegister`, naming the handler; `overrides` is proven test-only (the production call, with none, also builds). Also closes M0-10's own deferred integration assertion: production + filesystem media store fails to boot. A new `TelemetryInterceptor` + `instrumentPool` (both `platform/observability/`, no context file touched) give every request a three-level span tree — route, handler, `db.query` — sharing one `traceId`, proven against real Postgres across all three contexts in `app-factory.integration.spec.ts`.
- **M0-13** — `main.ts` (~30 lines: load config, exit non-zero with the config error's own message on failure, create, listen, trap `SIGTERM`/`SIGINT`, drain via `app.close()`) plus `platform/http/health.controller.ts` — `/healthz` liveness (200, touches nothing) and `/readyz` readiness (`SELECT 1`, 503 with a machine-readable reason when the database is unreachable), both absent from every OpenAPI document, neither authenticated. `app-factory.ts` gained `HealthModule.register(pool)` as a fourth root import and now closes the pool it built itself on `app.close()` (never a pool a test supplied via `overrides.pool`). SIGTERM draining proven for real: a real in-flight `/readyz` request against real Postgres completes 200 after `SIGTERM` fires, then `process.exit(0)` follows only once `app.close()` resolves — note the previous commit's message ("log M0-12 completion") wrongly bundled these M0-13 files in with that docs update; content is correct, the label is not.
- **M0-14** — The walking skeleton: one bearer token issued by the M0-05 stub reaches a real published item through resolver → controller → `GetPublishedItemHandler` → `PostgresItemRepository` → real Postgres, response carrying `X-Correlation-Id`, and every span `RecordingTelemetry` captured shares that id as its `traceId` in a three-level tree (route → `getPublishedItem` → `db.query`). The existing no-answer-key guarantee (F6/F35) is re-proven over live output. Unauthenticated → 401, expired token → 401, and the 401 check is shown fallible against a planted resolver bypass (a resolver that authenticates everyone turns the same unauthenticated request into a 200). The spec's header states plainly what it does not prove: nothing about a PII-scrubbed log line, because nothing on this request path writes one today (`createPrincipalResolver` is wired with no `logger`) — the span tree is the one observability surface asserted here. Full workspace green: `apps/api` 142 files / 3431 tests, `pnpm test`'s coverage run exits 0, no threshold violation.
- **Note** — commit `6d9c697` is labelled `docs: log M0-12 completion` but its diff also carries M0-13's source files (`main.ts`, `main.spec.ts`, `main.integration.spec.ts`, `platform/http/*`, the `app-factory.ts` pool-close change); a `git add` staged both changes before the commit ran. The content is correct and every file is where M0-13's own commit (`19ac1e8`) says it is; only this one commit's message misdescribes its diff, and it is not being rewritten. The close-out must not claim otherwise.
