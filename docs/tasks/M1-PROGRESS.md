# M1 — Implementation Progress

| Task | Date | Tests | Note |
|---|---|---|---|
| M1-01 | 2026-08-05 | 34 | `ConceptIdentity` aggregate; frozen instances, supersede returns a new instance so supersede-once is structural. |
| M1-02 | 2026-08-05 | 39 | `ConceptNode` (depth derived via `createRoot`/`createUnder`, never supplied) and `PrerequisiteEdge` (self-edge rejected). |
| M1-03 | 2026-08-05 | 46 | `TaxonomyVersion` structure + `taxonomy-invariants.ts`: one root per domain, no orphans, no parent or prerequisite cycles, no duplicate identities; violations name the offending nodes. |
| M1-04 | 2026-08-05 | 32 | `TaxonomyVersion` lifecycle: draft→published→superseded only, publication runs all invariants atomically and stamps `PrincipalRef`; every mutator rejected after publication. Shared kernel `packages/domain-types` added for `PrincipalRef`. |
| M1-05 | 2026-08-05 | 47 | `NumericAnswerSpec` (D-001): five modes, mode-required parameters enforced at construction, decimals carried as authored literals (not floats) so precision survives to M2. |
| M1-06 | 2026-08-05 | 23 | `TimingPolicy` + `NavigationPolicy`; the contradictory `sectionLocking`/`crossSectionNavigation` pair is rejected by `checkDeliveryPoliciesCompatible`, since neither VO alone can see both flags. |
| M1-07 | 2026-08-05 | 48 | `MarkingRuleSet` + `MarkingRule` + `Condition` (8) + `Award` (3); ALWAYS-terminated (F46), unique ids, order significant, unreachable rules warn. JEE Main set expressed as `UNATTEMPTED / EXACT_MATCH / ALWAYS(-1)` — same function as ASSESSMENT-ENGINE §2.4 rule 3, but F46-compliant. `AggregationSpec` deferred: undefined in the docs and outside M1-07 acceptance. |
| M1-08 | 2026-08-05 | 21 | `hashMarkingRuleSet`: sha256 over a canonical serialization (keys sorted, no whitespace, explicit numeric formatting, rule order preserved). Golden fixtures committed in `src/testing/golden/`. |
| M1-09 | 2026-08-05 | 23 | `SectionSpec` + `checkBlueprintConsistency`: contiguous ordinals, type mix sums to item count, section marks sum to the profile total, section timing only under `sectionLocking`. |
| M1-10 | 2026-08-05 | 25 | `Exam` aggregate: immutable upper-snake `code`, at most one active profile version per academic year, explicit deactivate to replace one. |
| M1-11 | 2026-08-05 | 36 | `ExamProfileVersion`: composes sections/policies/marking/tolerance/allowances; all five publication preconditions evaluated before any write; hash frozen at publication. JEE Advanced and NEET-shaped profiles publish with zero code change (EXT-01/EXT-03). |
| M1-12 | 2026-08-05 | 35 | `TaxonomyMapping` (6 kinds, cardinality enforced per kind) + `TaxonomyMigration` (references two versions, every mapped concept must exist in the right version, a concept may appear in at most one mapping). |
| M1-13 | 2026-08-05 | 16 | `runMigrationDryRun`: pure function of the migration; IDENTITY/RENAME auto-migrate, MOVE/SPLIT/MERGE/REMOVAL raise exceptions, unmapped source concepts raise UNMAPPED, stale mappings reported separately. Deterministic across 100 runs. |
| M1-14 | 2026-08-05 | 16 | Curriculum schema: 9 tables in `curriculum`, UUIDv7 PKs, `tenant_id` + `aggregate_version` on roots, every JSONB column with a `*_schema_version` sibling, reversible. Fitness F2/F5 asserted as SQL queries against the live database. `examFamily` added to `TaxonomyVersion` so the aggregate matches DATA-ARCHITECTURE §4. |
| M1-15 | 2026-08-05 | 19 | Published-row immutability triggers on `taxonomy_version`, `exam_profile_version` and their three child tables; rejected via ORM and via raw psql. Only `is_active`, `aggregate_version` and published→superseded may still move. |
| M1-16 | 2026-08-05 | 29 | Taxonomy repositories: whole-aggregate round-trip (600-node version), optimistic concurrency raising `Conflict`, partial-load rejection, snake_case↔camelCase confined to the repository files. `reconstitute` factories added to the three domain types. |
| M1-17 | 2026-08-05 | 18 | Exam + `ExamProfileVersion` repositories: rule set and hash round-trip exactly, JSONB validated against its owning domain type before the write, sections load in ordinal order, optimistic concurrency. `replaceSections` now re-validates item type allowances. |
| M1-18 | 2026-08-05 | 10 | Migration repository: mapping kinds/cardinality/order and dispositions round-trip, dry-run result stored as JSONB with its schema version, a migration in `executing` refuses modification. |
| M1-19 | 2026-08-05 | 26 | Taxonomy commands and handlers: six commands, each with a declared policy; `HandlerRegistry` refuses to register a policy-less handler (F36); authorization negative path per handler; audit record per mutation; transaction boundary verified against Postgres. NestJS deferred to M1-26 — nothing here needs HTTP or DI. Durable audit writer deferred to the identity schema. |
| M1-20 | 2026-08-05 | 15 | Exam profile commands and handlers: five commands, publication atomic (all preconditions before any write), step-up required to publish and supersede, activation enforced against the one-per-year rule. |
| M1-21 | 2026-08-05 | 13 | Migration commands and handlers: execution rejected without a dry run and while any exception is pending, step-up required, execution chunked through a `MigrationExecutor` port and resumable after an interruption. |
| M1-22 | 2026-08-05 | 17 | Seven curriculum queries, each with a policy; read models are plain DTOs (JSON round-trip asserted), `GetConceptSubtree` supports depth limiting, authorization negative path per query. |
| M1-24 | 2026-08-05 | 10 | Domain events + `platform.outbox_message` migration and `DrizzleOutboxEmitter`: event and aggregate change commit or roll back together, payloads carry identifiers only, event registry gives each event an analytics counterpart (F18). Fixed a flaky UUIDv7 ordering assertion in the M1-14 spec — only the 48-bit timestamp prefix is ordered. |
| M1-23 | 2026-08-05 | 15 | `contexts/curriculum/public/index.ts` exports commands, queries and events only — value objects as read-only DTO types. Boundary fitness function (F1/F2) with two planted violations proving the rules fire; barrel surface asserted. dependency-cruiser rejected: it refuses to run on Node 23, so the rules are evaluated by an in-repo checker (`src/fitness/boundary-rules.ts`, `pnpm fitness`). |
| M1-25 | 2026-08-05 | 24 | `packages/contracts`: OpenAPI 3.1 spec with 22 operations covering all 15 commands and 7 queries, cursor-only pagination, allowlisted sort/filter enums, RFC 9457 problems with the closed code set, camelCase throughout. Types generated by openapi-typescript. F15 asserted by reconciling `x-handler` against the handler registry. |
| M1-26 | 2026-08-05 | 19 | NestJS `CurriculumController` (22 routes) + Zod request schemas + RFC 9457 mapping: every error kind maps to its status, 409 on stale `If-Match`, 428 when it is absent, 400 with field detail on a malformed body, `Idempotency-Key` required on publish/execute, correlation id echoed on success and failure. Principal resolution is a port — JWT verification belongs to the Identity context. |
| M1-27 | 2026-08-05 | 12 | YAML taxonomy import format (`tools/seed/schema/taxonomy.schema.json`, schemaVersion 1) + loader: whole-file validation before any write, per-record errors with path and line, idempotent re-runs via key-derived ids, always produces a draft. `tools/seed` is now a workspace package with its own vitest and tsconfig. |
| M1-28 | 2026-08-05 | 16 | JEE Main 2026 taxonomy dataset: 608 concepts across Physics/Chemistry/Mathematics in NTA subject→chapter→topic shape, chapter weights summing to exactly 1.0 per subject, 47 acyclic prerequisite edges; loads, validates and publishes. **SME sign-off outstanding** — recorded in the file header. |
| M1-29 | 2026-08-05 | 11 | JEE Main 2026 exam profile (`tools/seed/data/jee-main-2026.profile.yaml`) + profile schema and loader: 3×25 items (20 MCQ + 5 numeric), 300 marks, 180-minute single timer, three-rule marking set, tolerance defaults; publishes with every precondition met and matches a committed golden hash. |
| M1-30 | 2026-08-05 | 22 | **NEET UG 2026 profile — the EXT-01 proof.** 180 items (45/45/90), 720 marks, 200 minutes, single-correct MCQ only; publishes with no schema migration and no change outside `tools/seed/data/`, asserted by a `git diff --name-only` check against the pre-NEET commit. Its marking set hashes identically to the ASSESSMENT-ENGINE §2.4 reference set. |
| M1-31 | 2026-08-05 | 7 | `pnpm seed` loads and publishes both taxonomies and both profiles on a clean database in ~0.3s (budget 60s), is idempotent (second run reports `unchanged` with identical row counts), and attributes every publication to a system `PrincipalRef`. |
