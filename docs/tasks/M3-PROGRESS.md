# M3 — Implementation Progress

| Task | Date | Tests | Note |
|---|---|---|---|
| M3-01 | 2026-08-09 | 56 | Content context anatomy (five directories) + `Result` and the closed error taxonomy, five kinds narrowed from the platform ten by `satisfies`. `ContentError` carries an optional `location` — an author who cannot find the problem submits nothing (UX §10.1) — and omits the key entirely rather than serializing `undefined`, which reads as "the problem is nowhere". Three domain guards each proven against a planted violation. **Fixed a real defect in M2's F1/F2 boundary checker:** it matched `from '...'` against raw file text, so the doc-comment phrase `separates a usable error from "invalid item"` was reported as an import and F2 failed. Comments are now stripped by a shared string-aware scanner (`src/fitness/source-scan.ts`) before any guard reads a file, with the regression test in `boundary-rules.spec.ts`. |
| M3-02 | 2026-08-09 | 67 | `ContentBody` — six block kinds, four inline kinds, both closed. `textAlternative` is enforced in the constructor rather than at publication: a node that can exist without one eventually reaches a student without one, and ACC-02 asks for reading order verified *per notation class*, which an optional field makes unverifiable. Rendered markup is caught by shape — `x<sup>2</sup>` in a text value is HTML stored in a structured field and renders as literal text on print, which is the exact divergence INV-14 exists to prevent — while a bare `a < b` still parses, since refusing that would push authors toward images. Every failure carries a location (`blocks[0].rows[1]`), because the validation panel has to point at something. List nesting is bounded at 3, which also makes the recursion provably terminating. |
| M3-03 | 2026-08-09 | 36 | The three projections DATA-ARCHITECTURE §2 requires, written from the document rather than alongside it. `plainText` renders notation as its `textAlternative`, never its LaTeX — `\frac{1}{2}mv^2` in a search index matches nothing a human types, and this string doubles as the closest thing the model has to a statement of reading order. `notationTerms` drops backslashed commands as structure and keeps `H2SO4` whole as `h2`/`so4`: my first test expected `h`/`2`/`so`/`4`, which would match half the periodic table — the tokenizer was right and the expectation was wrong. `referencedMediaIds` yields one id per asset however many times it is mentioned, because a usage graph that counts mentions answers "is this in use?" wrongly the moment a caption is edited. All three deterministic, asserted over 100 runs and by byte-identical JSON. |
| M3-04 | 2026-08-09 | 25 | `TaxonomyTag`. The load-bearing rule is that a tag cannot be constructed without the `taxonomyVersionId` it was authored against — a tag naming only a concept is uninterpretable after a syllabus revision and gives FR-QM-13's migration nothing to map from, so the migration path stays possible by construction rather than by everyone remembering. A set binds to exactly one taxonomy version: two versions means half an item migrates and nothing downstream can say which taxonomy it is tagged under, which is worse than refusing. Exactly one primary, since coverage reporting and search ranking both key on it. Concept identities cross as values, never as a join (§9 rule 3). |
| M3-05 | 2026-08-09 | 42 | `Provenance`, closed source-type set. AI provenance requires all four generation fields — an item labelled `ai_generated` with no model, prompt, run or confidence is one nobody can audit or recall when a model turns out to be wrong, and that is half of what makes INV-01 checkable (M3-11 is the other half). The reverse is refused too: a human-sourced item carrying model fields is either mislabelled AI content or a copy-paste, and both defeat the same audit. An unknown source type is refused rather than defaulted, because guessing `original` would launder a third party's paper into the corpus as our own work. `sourceYear`'s upper bound is supplied, not read from a clock. |
| M3-06 | 2026-08-09 | 38 | `LicensingStatus`. `unresolved` is the **default for a new draft**, not just a value an author may pick: defaulting the other way is how a corpus ends up holding content nobody can account for, which is precisely the question DECISIONS §D item 2 has open against existential risk R5. An expiry on `owned` or `public_domain` is refused rather than ignored — the case worth catching is a licence mislabelled as ownership, and silently dropping the field hides it. Expiry is inclusive: a licence expiring at *t* is expired at *t*, because serving on the boundary is the same exposure as serving without one. The instant is supplied, so a publication decision never depends on when the process happened to run. |
| M3-07 | 2026-08-09 | 45 | `ResponseSpecification` — presentation and key together, because they are authored together and separating them invites the two to disagree: an option list edited without its `correctOptionId` is a wrong key, not a layout change. **An option body is a `ContentBody`, not a string** — an option routinely *is* an equation, and a string field forces either an image of it or markup smuggled into text, both INV-14 violations that cannot be walked back. Ordinal gaps are refused because a gap means an option was deleted and the key may now point at a position nobody sees. A multi-correct item with every option correct is refused: it pays full marks for selecting everything. Mode-required numeric parameters are deliberately *not* validated here — M3-08 delegates to scoring's own constructor, since two validators for one rule is how the editor and the executor come to disagree about what is publishable. |
| M3-08 | 2026-08-09 | 40 | **The M3→M2 seam.** `toAnswerKeyData` projects the key half of a `ResponseSpecification`; validation is scoring's own (`isKnownItemType`, `createAnswerKey`), never a hand-written shape check — a local check would pass keys the executor refuses later, at scoring time, for a real candidate. Lives in `application/` because `domain/` imports nothing, and a seam is not a domain concept anyway. **Widened `scoring/public/` to export `KEY_KIND_BY_ITEM_TYPE` and `isKnownItemType`**: the ratified DEC-3 amendment wants a test that fails on *either* side drifting, and the barrel exposed no vocabulary to compare against — precisely the gap the M2 close-out built the seam spec to force. Dropped a `createScoringInput` probe I had written for the variant check: `toAnswerKeyData` derives the variant from the item type, so its failure branch was unreachable, and carrying dead code to look thorough is what ADR-0008 warns against. The pairing is asserted in the spec instead, where a real `createScoringInput` accepts all four types. Authored decimal literals cross as text — `0.1` and `0.1000` stay distinguishable. |
| M3-09 | 2026-08-09 | 44 | `ItemVersion`, immutable with **no mutator to remove later** — INV-03 holds because there is nothing to call, not because a rule says so. An edit goes through `deriveDraft`, which increments `versionNo`, returns a new object, leaves the original byte-identical, and carries forward stem, specification, tags, provenance and licensing: an edit that silently dropped any of those would produce a version failing publication for reasons the author never touched. `authoredBy` on a derived version is the principal making *this* edit — the audit trail follows the change, not the lineage. `stimulusVersionRef` pins a stimulus **version**, so a shared passage edited elsewhere cannot retroactively change what an item asked. Item type and specification type are cross-checked, since letting them differ means the key is scored under one type and presented under another. Licensing defaults to `unresolved` when unstated. `localeVariants` deliberately absent until M3-16. |
| M3-10 | 2026-08-09 | 168 | `Item` + the lifecycle state machine, with **[ADR-0010](../adr/ADR-0010-content-owns-the-lifecycle-state-machine.md)** recording the divergence from ROADMAP M4's placement — you cannot block a transition you do not own, and the two ways of proceeding without it are enforcing publication rules in the Studio UI (forbidden) or shipping an unguarded publication path for a whole milestone. The machine is a table, so "every transition it does not name is refused" is a property of the data; the spec sweeps all **8 × 9 = 72 cells** against a legal list written out independently, 13 permitted and 59 refused with `RuleViolation` naming what was attempted. `transitionItem` refuses `publish` outright and points at `publishVersion`, so INV-07 is not one forgotten argument away from bypassed. Publishing supersedes in one operation — no window with two current versions, no way to end with none while intending to swap. `retired` is terminal because FR-QM-07 rule 2 keeps history pointing at it. Coverage found a real gap: `latestVersionOf` was only ever tested on ascending input, and storage order is not version order. |
| M3-11 | 2026-08-09 | 44 | **Publication preconditions** — INV-07, INV-12, INV-01, INV-14, all in the domain. Every unmet precondition is reported at once, not the first: an author told about one thing per resubmission stops resubmitting (UX §10.1). Facts the domain cannot know (solution availability, render verdict, the instant) are **supplied as evidence, not as booleans** — a caller wanting to bypass a precondition must fabricate a signature or a verdict, which is a far more visible act than passing `true`. INV-01 is asserted directly as well as inside the list: AI-sourced provenance requires a *human* signature, so there is no code path from a model to a published item. **Two real defects found.** (1) `checkPublishable` threw on a version with no provenance — the domain may never throw (§8), least of all on the path deciding what reaches a student. (2) The F1/F2 scanner matched `from` inside a **string literal**, so the message `'…records where it came from'` reported an import of `", "` — the same class as M3-01's comment defect, one layer down. Fixing it exposed a third: `boundary-rules.ts` held a **duplicate copy** of the patterns, so the gate F1/F2 actually runs kept the loose version while its own helper was tightened. Collapsed to one implementation. Also removed an unreachable `??` fallback. |
| M3-12 | 2026-08-09 | 50 | `Stimulus` + `StimulusVersion`, reusing `Item`'s lifecycle table unchanged — a second state machine that differed by accident is exactly what a shared table prevents. **One deliberate divergence from `Item`: a new version is permitted while published.** Editing a published passage is the ordinary case (a typo, a clarification), and FR-TCH-03 rule 2 answers it with a new version that existing associations do not follow; refusing the edit would push authors back to pasting the passage per item, which is the error DOMAIN-MODEL calls "the category's canonical fatal error". A test proves an existing association still resolves to the old version after a newer one is added, and only moves when the new one is published deliberately. Retirement while referenced is refused, and **not knowing the count is refused too** — defaulting it to zero would make FR-TCH-03 rule 3 advisory. Coverage caught the same `latestVersionOf` gap as M3-10, mirrored. |
| M3-13 | 2026-08-09 | 61 | `Solution` + `SolutionVersion`, steps, distractor analyses, alternate approaches. Targets an item **version**, not an item (FR-TCH-04 rule 3) — the correctness a solution explains belongs to a specific key, so a solution for version 1 says nothing about version 2, and pinning the target is what lets an explanation be rewritten a year later without any attempt's meaning changing. The "complete" grade (FR-TCH-04 rule 2) is **computed from the analyses actually present**, never asserted: a grade an author can claim is a grade an author will claim. `unanalysedDistractors` names the gaps so the editor can prompt for the misconception while the author still has the item in their head (UX §10.1). A numeric final answer stays the authored literal — M3-14 compares it through the item's own `NumericAnswerSpec`, and reading it as a double here would decide agreement on a value nobody wrote. Key agreement itself is deliberately *not* checked here: reaching across to the item's key would be the coupling D5 exists to avoid. |
| M3-14 | 2026-08-10 | 31 | Final-answer / key agreement (D5), **blocking** — a solution contradicting the key is the defect that produces answer-key challenges: the learner reads a derivation ending in 9.8, answers 9.8, is marked wrong, and is entirely right to dispute it. I first wrote a textual comparison whose doc comment claimed the executor decided it; both halves were wrong. **Widened `scoring/public/` to export `evaluateExactMatch`** (scoring's own `evaluateExactness`, previously private) so tolerance, comparison mode, units, accepted forms and normalization all apply exactly as they will at scoring time. Agreement means *the executor would mark this correct*: a value inside the item's own tolerance band agrees, because a learner answering it is not marked wrong and no defect exists — authorial imprecision is a review matter, not a scoring one. `indeterminate` is explicitly not agreement. Removed a second `createAnswerKey` call whose failure branch was unreachable; `projectValidatedAnswerKey` now shares the one validation. **Environment note:** the Homebrew Postgres data directory was gone at this point (`/opt/homebrew/var/postgresql@16` missing entirely); re-initialised a fresh cluster on 5433 per ADR-0004 and both databases, after which all 2302 tests pass. |
| M3-15 | 2026-08-10 | 78 | `MediaAsset`. **Alt text is enforced at construction, not at publication** — the same argument as `textAlternative` in M3-02: putting the check at publication means assets without it exist, get referenced, and are then either published under pressure or fixed by whoever notices. An asset that cannot exist without alt text cannot reach a student without it, and a test proves no asset type has an escape hatch. Everything except a `photograph` additionally needs a long description, because a chart or reaction scheme carries *information* and "a graph of velocity against time" tells a screen-reader user the figure exists and nothing it shows. **No byte-bearing field exists on the module**, asserted by source scan — the failure being prevented is a base64 blob in a JSONB column, invisible until the table is unmanageable. MIME allowlist closed; `image/svg+xml` is in it because diagrams belong there, with sanitization recorded as the storage adapter's job (M3-27). Retirement refused while referenced, and refused when the count is simply unknown. |
| M3-16 | 2026-08-10 | 41 | `LocaleVariant` — modeled per FR-QM-11, delivered in H1. **A variant carries no correctness**: no key, no numeric specification, no correct-option marker, enforced structurally (there is nowhere on the type to put one) and asserted by a source scan. A translated key would be a second answer to the same question, diverging silently the first time either side is corrected. A partial translation is refused, since showing some options in one language and some in another is worse than showing none translated. Attestation is fidelity, not adjudication (D-005) — three states, not the lifecycle's eight — and a translator cannot attest to their own work, on the same reasoning as INV-12. `applySourceVersionChange` implements rule 3 as a pure function and **drops the attestation**, because it was to a fidelity that no longer means what it meant. Two tests keep "modeled" from becoming "half-shipped": no other module in the context references it, and `ItemVersion` still declares no `localeVariants` field. |
| M3-17 | 2026-08-10 | 45 | Pre-submission validation (FR-TCH-07). Blocking and warning code sets are **disjoint and exhaustive**, asserted both ways and over every code the validator actually emits — a code that is neither is one the submit gate cannot treat, and the safe reading of "unknown" would be a refusal nobody can explain. Everything is reported at once and every finding carries a location (`version.responseSpec.options[c]`), because "invalid item" is a message an author cannot act on. Missing numeric tolerance is a **separate finding from a bad key**, so the author is told which field to fill rather than that the key is wrong. Warnings never block: a probable duplicate is advisory because genuine variants are legitimate (FR-QM-04 rule 2). **Duplicate detection reports `not_evaluated`, and `describeDuplicateCheck` says "has not run"** — a report claiming none found when the check never ran is a lie a reviewer acts on. Dropped a dead `isEmptyBody` helper; coverage caught a short-circuit that never evaluated `rangeMax`. |
| M3-18 | 2026-08-10 | 22 | Six content events, past tense, **identifiers only**. The temptation was real — `ItemPublished` carrying the stem would save every consumer a fetch — and it is refused because the outbox drains to analytics (P4/D17), which is exactly where a key must never arrive. A spec serializes every payload and scans for nineteen content-, key- and PII-bearing field names. `SolutionPublished` carries no explanation text for the same reason plus entitlement: routing it through an event would bypass the gate INV-08 leaves in place for everything above basic correctness. F18 registry asserts each type is registered exactly once, has **either** a counterpart **or** a written exemption (never neither, never both), and that every named counterpart actually exists in EVENT-TAXONOMY — a reconciliation against a name nobody publishes proves nothing. Made `registrationFor` return `undefined` rather than falling back to entry zero: handing back another event's registration would emit under the wrong analytics name. **Track A complete.** |
| M3-19 | 2026-08-10 | 42 | Content schema: 19 tables, up/down/up verified against a live database rather than argued from the file. **F5 holds and the check is not vacuous** — a second test asserts there were ≥9 JSONB columns to check, since an empty orphan list proves nothing if nothing was scanned. §9 rule 3 asserted by catalogue query over `pg_constraint`: no FK leaves the schema, and Curriculum's identifiers are carried as values so a tag stays interpretable after its taxonomy version is superseded. `alt_text NOT NULL` with a non-blank check — **neither the type check nor the database check is sufficient alone**; the type stops the code path, the database stops everything else. Provenance constraints enforce INV-01 in both directions at the database. `lifecycle_state` is one shared domain, not respelled per table. Two test-side corrections: my singular-name heuristic called `distractor_analysis` plural (it isn't — the plural is `analyses`), and Postgres truncated a generated composite FK name at 63 chars, so both matching-pair constraints are now named explicitly — a constraint nobody can name is one no test can assert on. |
| M3-20 | 2026-08-10 | 25 | Published-version immutability (INV-03) at the database. **Deliberately not scoring's blanket append-only rule**: a content version is editable while a draft and immutable from publication, because that is what the draft state is *for* — a blanket rule would make authoring impossible. The trigger keys on a `published_at` column added here, and the test is **"ever published", not "currently published"**: a superseded version must still reproduce, since an attempt is pinned to it (INV-04). A version's *parts* freeze with it — options, tags, provenance, numeric spec, steps, media edges — because otherwise a published item's **answer key could be edited without touching the row that claims to be immutable**, which is exactly the hole INV-03 exists to close. Clearing `published_at` to unlock a version is refused. §9 rule 11 is adapted and the adaptation is stated: UPDATE/DELETE grants are *kept* (drafts need them), so TRUNCATE — which a row trigger cannot see — is the one path where the grant rather than the trigger is the control. The grant test records honestly that the deployment role does not exist locally (ADR-0004) rather than passing as if checked. |
| M3-21 | 2026-08-10 | 44 | `Item` repository: one aggregate, one transaction across nine tables. **Projections are recomputed on every save, never accepted from the caller** — a supplied projection can disagree with the document it summarizes, and the disagreement is invisible until an author swears an item exists and search says otherwise; a test asserts the stored value equals a fresh recomputation. Decimal literals cross as text and `0.1` stays distinguishable from `0.1000` through storage. Added `content_licensing` per DATA-ARCHITECTURE §4 (the doc specifies a table, not per-version columns) and amended the M3-19 migration rather than stacking a second one, since nothing is deployed. **Three defects found by the tests.** (1) `jsonb` does not preserve key order, so my byte-identity assertion was wrong — the *document* round-trips, the *serialization* does not, now pinned by its own test so nobody later assumes otherwise. (2) The save inserted a published item before its version existed, tripping `item_published_names_a_version`; state and published version now move together in one final statement, since a CHECK cannot be deferred. (3) A `finally` carrying an unreachable abrupt-completion path — the same artifact M2-20 hit, resolved the same way. Fixtures use readable ids that could never be stored in `uuid` columns, so the integration spec supplies real ones. |
| M3-22 | 2026-08-10 | 26 | `Stimulus` repository. **Found and fixed a serious gap in M3-20/21 while writing it: no repository set `published_at`, so the immutability trigger never armed for anything the application wrote** — INV-03 held against `psql` and not against us, which is exactly backwards. Both repositories now stamp it on publish, only while still NULL so republishing a superseded version cannot move the instant it was first published. Four new tests prove a version published *through the repository* refuses edits to its body and to its answer key. FR-TCH-03 rule 2 is proven end to end against the database: an item authored against stimulus version 1 still reads version 1 after version 2 is published. The reference count stays on `ItemRepository` — it counts items, and one query with one implementation beats putting it where its caller sits. A suspended item still counts, since it can be reinstated and would break if the stimulus were retired underneath it. |
| M3-23 | 2026-08-10 | 26 | `Solution` repository. Steps return in **ordinal order**, never insertion order and never whatever the database happens to give — asserted by storing them out of order and reading them back sorted. The final answer is stored as the authored assertion in JSONB (polymorphic by item type, only ever read whole — DATA-ARCHITECTURE §2's own rule), with the numeric variant keeping its **decimal literal as text inside the document**, since M3-14 compares it through the item's `NumericAnswerSpec` and a float would decide agreement on a value nobody wrote. `findPublishedForItemVersion` is keyed on the **version**, so a solution written for version 1 cannot license publishing version 2 — the precondition M3-11 consumes. Media referenced from a step or a misconception joins the usage graph under `solution_version`. Removed one unreachable guard: a solution naming a published version it does not hold is already refused by `reconstituteSolution`, so re-checking in the lookup was a branch nothing could reach; the spec asserts the corruption is still reported. |
| M3-24 | 2026-08-10 | 25 | `MediaAsset` repository and the usage-graph count. **`countReferencingPublishedContent` spans items, stimuli and solutions in one query** — counting each kind separately and adding would give three chances to forget one, and the question FR-QM-06 rule 3 asks is "is anything published using this", not "how many of each". Draft content does not count (it is not circulating); a **suspended** item does, because it stays pinned and would break on reinstatement if the asset were retired underneath it. Each owner repository writes its own edges as it saves a version, since the edge is derived from *that* version's document; this file only reads them. The no-byte-field scan runs against the repository too, not just the domain — that is the check that would catch a base64 blob before it becomes a table nobody can migrate. **Track B complete.** |
| M3-25 | 2026-08-12 | 49 | Item authoring commands, handlers and autosave, plus the `authorization.ts` / `handler-registry.ts` / `ports.ts` triple Track C needs (mirrored from scoring's, not imported — §9 rule 1). **Autosave edits the draft version in place**, because a version per keystroke burst makes history unreadable and leaves a reviewer nothing to diff; a *revision* after review is `deriveDraft` + `addVersion` (FR-TCH-09 rule 1), which is a different act with a different command. That exposed two real gaps. (1) `#saveVersion` returned early for **any** existing version, so an edited draft never persisted — the repository refused an edit the database explicitly permits (M3-20 keys immutability on `published_at`, not on existence), and the author's work vanished silently on save. It now rewrites an unpublished version and still skips a published one. (2) The rewrite **reconciles** the option, tag, provenance, licensing and media-edge sets rather than accumulating them: a media reference the author deleted had to leave the usage graph or the asset stays unretirable forever (FR-QM-06 rule 3). Added `replaceDraftVersion` to `item.ts` rather than assembling the aggregate in the handler — reconstituting an item by hand in `application/` is domain logic in the wrong layer (§1), and it left a branch nothing could reach. Idempotency is a **port**, not content-equality: content-equality cannot tell a retry from an author typing a character and deleting it again. The key is remembered only after the write lands, so a dropped connection stays retryable. `authoredBy` does *not* move on an in-place edit — Content Ops fixing a typo must not take ownership of somebody's draft and lock the author out (FR-TCH-06 rule 1) — but it does on a derived version, where the audit trail follows the change. Added `updated_at` to `item_version` (amending M3-19, nothing deployed) stamped by infrastructure the way `published_at` is; `ItemVersion` deliberately does not carry it, since a snapshot has one authored instant. Debt **D22**: the idempotency store is process-local, so a retry landing on another instance is a redundant rewrite of identical content rather than a no-op. |
| M3-26 | 2026-08-12 | 60 | Stimulus and solution commands and handlers. **Attachment resolves the version; it does not take one** — a caller naming a version could pin one the author never saw, and FR-TCH-03 rule 2 is only a property if the resolution happens once, here. `pinStimulusVersion` is a domain function for that reason rather than a spread in the handler. Attachment prefers the published version and falls back to the latest draft: an author writing a passage and its items in one sitting has published nothing, and refusing would invert the workflow the aggregate exists to support — what stops unapproved content circulating is the *item's* publication gate. **Final-answer agreement runs on every solution save, not only at publication**, and the update path re-reads the item rather than trusting what was true at creation, because the key can change under a solution written against the old one. Both repositories got the same draft-rewrite fix as M3-21 (they had the identical early-return), with steps, analyses, approaches and media edges reconciled rather than accumulated, plus `updated_at`. Subject scope (FR-TCH-01 rule 1) is carried as a `subject:<name>` entry narrowing an authoring role, per SECURITY-ARCHITECTURE §4's "roles are additive; scope narrows them" — Content Ops is unscoped there and so is unscoped here. **The subject is declared on the command and is not cross-checked against the content**, because nothing in the model records the subject of a passage and `curriculum/public/` exposes no concept→subject-domain lookup: this refuses a Chemistry author reaching for Physics, and does not catch an in-scope author mistagging their own work. Debt **D23**. Noted for M3-28: no publication precondition requires the pinned stimulus to be published, so a published item can name a draft passage. |
| M3-27 | 2026-08-12 | 29 | Media commands and the `MediaStore` port. **A command names a `storageKey` and never bytes**: the upload edge puts the object, the store answers with a key, and registration records what the store already holds. **The checksum is read from the store, never from the caller** — a caller-supplied checksum verifies nothing, because whoever replaced the object would supply the new one — and `checkStoredObjectUnchanged` re-reads it before publication, which is what makes a figure swapped after review detectable rather than invisible. The handler also refuses a declared MIME type the stored object contradicts, since the allowlist is worthless if the declaration is the only thing checked against it. Retirement resolves the reference count from M3-24 and **refuses when the count cannot be resolved at all**, since unknown is not zero (FR-QM-06 rule 3); it is Content Ops only, because retiring a figure removes it from everything downstream. `application/ports.ts` is the single named exemption in the context-wide byte scan, and that is the point of the rule — `MediaStore` names `Uint8Array` there so nothing else has to. Dropped `data` from the context-wide field pattern: across a whole context it matches `readonly data: AnswerKeyData`, and a guard that cries wolf gets exempted into uselessness. Removed one branch from `InMemoryMediaStore.replace` — istanbul merged the two vitest projects' branch maps into a phantom uncovered arm, and a guard for "replace something not there" was not worth keeping to argue with the merge. `filesMatching` gained an `exclude` list so an exemption must be named at the call site. |
| M3-28 | 2026-08-12 | 103 | Lifecycle commands and permission gates — thirteen handlers, one per act, because a generic "set state" would be one permission gate for thirteen decisions. **The publication precondition needed evidence to consume, and nothing stored any**, so M3-28 adds `content.review_decision` and its repository: the scope boundary gives M4 the review *workspace* (assignment, ageing, the capture screen), and a precondition that depends on another milestone's storage is not a precondition. The table is append-only in intent and polymorphic on owner, so a reviewer who changes their mind records a second decision and the history FR-TCH-09 rule 1 needs survives; `findApprovalFor` reads the most recent *approving* one, so approved→returned→approved resolves to the approval that stands. A decision that sends work back requires a justification, in the constructor **and** as a CHECK — an author told only "rejected" has nothing to act on. `toReviewerSignature` returns nothing for a non-approval, which is what stops a rejected version publishing. `PublishItemVersion` resolves the signature, the published solution and its agreement with the key, the render verdict and the licence's instant, and hands them to `checkPublishable`; it decides nothing, and the precondition list travels as structured `detail` so the validation panel groups by code rather than reading a message. Every refusal is proven end to end, including the D5 case — a solution published against the old key, the key then edited, publication refused — and INV-12 and INV-01 are proven by writing the offending decision straight to the record, so the guarantee does not rest on one code path staying correct. `RenderValidator` and `ReviewProgress` are ports: M3-38 supplies the first, M4 the second, and **until M4 lands nothing claims a version, so withdrawal while `in_review` is always permitted** — stated rather than hidden. `#hydrate` on the review repository is total because every invariant the constructor enforces is also a column constraint; re-running it would add a branch nothing can reach. Publication and retirement require step-up, per SECURITY-ARCHITECTURE §4. **Still open and now visible: no publication precondition requires a pinned stimulus to be published**, so a published item can name a draft passage — debt **D24**. |
| M3-29 | 2026-08-12 | 33 | Content queries, two view families (DEC-4). **Delivery views are built field by field, never by spreading a domain object and deleting what should not be there** — a spread carries whatever the source gains next year, and the field it would carry first is the one somebody adds next to `correctOptionId`. So the matching *pairing* is simply not read (a pairing is the key), the numeric expected value, tolerance and range are not read, and only what an input box needs to label itself — canonical unit, required flag, accepted forms — crosses. Asserted the M2-24 way: a real published item through a real repository, serialized, scanned for fourteen key-bearing names, with the same item's authoring view scanned as the non-vacuity control. **Found and fixed a real leak:** `GetPublishedItem` first read the published *version*, which a **suspended** item still names — history is retained (FR-QM-01 rule 5) and that retained pointer would have kept serving content somebody had withdrawn from students. It now reads the state, takes the version off the aggregate already loaded, and refuses anything not `published`; the same for stimuli. That also removed a second repository call whose failure branch nothing could reach. `GetPublishedSolution` carries the derivation and **not** `finalAnswerAssertion`: as a structured field that is a correct-option marker or an expected value, which is the key by another name — the steps state the answer in prose, which is what INV-08 grants. Entitlement is a distinct kind from authorization and is consulted **only** for the paid depth, so an entitlement service that is down cannot withhold basic correctness (INV-08). A learner reaching an authoring query is `Authorization`, never an empty result. `solutionExists` in the validation report reads as *published*, because a draft explanation nobody approved would report the gap as closed while INV-08 still blocks; `outOfScopeConceptIds` is empty and says why (D23). Added `MediaAssetRepository.list`, which reports an asset it cannot reconstitute rather than returning a library smaller than the table. |
| M3-30 | 2026-08-12 | 36 | Bulk import (DEC-7). JSON Lines, header first, **and the header's licensing is not defaultable**: FR-QM-10 rule 2 rejects undeclared records, and a format that lets a batch omit the declaration turns "undeclared" into "unresolved and imported anyway" — the exact question DECISIONS §D item 2 has open. A malformed *record* is reported and the next line is read; a malformed *header* stops the batch, because every record inherits from it. **Parsing is separate from constructing**: this module turns bytes into records and decides nothing about validity, so `createItemVersion` and `createItem` — the interactive path's own constructors — are the only judges, and import cannot create a draft the editor would refuse to save. Asserted by the rejection codes being the domain's own (`TAGS_REQUIRED`, `CORRECT_OPTION_UNKNOWN`, `DIFFICULTY_BAND_UNKNOWN`, `LICENSE_REF_REQUIRED`) rather than a parallel vocabulary. **"Every record enters as draft" is unreachability, not a check** — `createItem` starts at draft and the handler calls no transition, asserted by a source scan for `transitionItem`/`publishVersion`. Each record saves on its own, so a failure at record 400 leaves 399 standing, proven with a good record *after* a bad one. The path is explicitly for **previous-year corpora** (FR-QM-10's purpose), so provenance is `previous_year` and a record that does not name its year is rejected rather than imported with the year missing. `duplicateCheckState` is `deferred` and the description says "has not run" — a report claiming none found when nothing ran is a claim a reviewer acts on, so the spec asserts the wording carries no "none". Policy is Author **and** Content Ops per FR-QM-10's actors, with the subject scope doing the narrowing. |
| M3-31 | 2026-08-12 | 22 | Public barrel and boundary enforcement. Exports commands, queries, events, the closed vocabularies a consumer has to agree with, and `ContentBody` **with its constructor** — a consumer that hand-built a body would build one the renderer has never been asked to render (DEC-2). **`AnswerKeyData` is the one export carrying key material**, and it crosses *here*, named, with the server-side-only note attached, rather than by M6 reaching into `content/domain` for it — the same coupling nobody would notice. Re-exported from `scoring/public/` rather than re-declared, so the two cannot drift. The **M4 seam spec** imports the barrel and nothing else: it constructs every lifecycle command a review workspace drives, types both view families, reads the finding and precondition code sets, and asserts the barrel's value-level export list exactly — so an export removed or quietly added is a failing test rather than a discovery in M4. Two planted-violation fixtures added for content specifically: `as-content-boundary` reaches into another context's domain, infrastructure *and* application (three distinct F1 firings, because a barrel stands in front of three places), and `as-content-domain/planted-outward-import` reaches out **through a well-behaved barrel** — which F2 still refuses, since "domain imports nothing" has no exception for a polite import. That is the rule that keeps `answer-key-projection.ts` in `application/` rather than beside the response specification, where it would be more convenient and wrong. |
| M3-32 | 2026-08-12 | 29 | Outbox emission. **The repositories take the events with the aggregate**, so joining the transaction is not something a caller can forget: `save(aggregate, events)` writes both or neither, and the rollback test proves an event goes with a refused publication rather than being emitted by a handler that already committed. Payload inspection runs on the **stored row**, not on the object in memory that produced it, because the outbox drains to analytics (P4/D17) and what is in the row is what leaves. Nineteen field names scanned across all six event types, plus a positive check that every payload key is an identifier, a version number, a closed-vocabulary member, or one of the two governance justifications — so a new field has to be defended rather than merely not match a blocklist. **Closed a real hole while wiring it: `MediaAssetPublished` had no producer, and neither did any path to a published media asset** — so `countReferencingPublishedContent` would have been 0 forever and `RetireMediaAsset` would always have succeeded. Added the three media transitions, which also gave M3-27's `checkStoredObjectUnchanged` its production caller: an asset whose object was swapped after review is refused at publication, proven end to end. `review_decision` gained `media_asset_version` as an owner type, since an asset carries the ACC-03 contract and is reviewed like anything else that reaches a student. Removed two branches nothing could reach — a `??` fallback on a version `transitionStimulus` had already validated, and `supersedesItemVersionId`, which has no producer because publication is legal only from `approved` and an `Item` refuses a new version past draft. **That last one is a real gap, not just a dead branch: a published item cannot be revised at all today** — debt **D25**. |
| M3-33 | 2026-08-12 | 32 | OpenAPI contract and **[ADR-0009](../adr/ADR-0009-authoring-dtos-carry-the-answer-key.md)**. 37 operations, each naming a handler that exists and declares a policy, reconciled against the registry in both directions. **The key-material scan runs over the parsed, `$ref`-resolved document, not over its text** — a delivery response referencing a schema that references a key-bearing one is a leak, and a text scan of the response block would never see it. All three ratified conditions are enforced: the authoring route list is an enumerated `x-authoring-routes` constant asserted against the document *both ways* (a new authoring path not on the list fails; a listed path that disappears fails); the check asserts keys **absent** from every delivery schema **and present** on the authoring schemas that are supposed to carry them, because a one-directional check passes when somebody silently removes the key from the editor; and no `Authoring*` schema is reachable from a non-authoring operation. The import-graph half of condition 3 needs a controller layer, so it is **stated in the ADR and asserted at M3-44** rather than assumed closed — a test pins that the ADR says so. **M2's blanket F6/F35 check started failing, which is exactly the moment ADR-0009 exists for**; it is amended with a single enumerated exemption, and two new tests pin that the exemption is one file and that the exempted document genuinely carries the key — so the amendment cannot quietly become "keys allowed where needed". **D18 closed for this context**: `scripts/generate-zod.mjs` derives 65 Zod schemas from the document, and the spec regenerates into a temp file and compares byte for byte, so editing either side alone fails. **D7 is NOT closed and the spec says so in its own describe block**: the official 3.1 meta-schema is not in the dependency tree and cannot be fetched here, so what ships is a strictly weaker structural check — required top-level fields, every `$ref` resolvable, every response described, no unreferenced schema — named as not-the-meta-schema so a green suite is not read as conformance. |
| M3-34 | 2026-08-12 | 23 | Content controllers — 34 authoring routes and 3 delivery routes, through a real Nest application. **The DTO modules are split so ADR-0009 condition 3 is a fact about imports rather than a fact about names**: `authoring-schemas.ts` carries the key and is imported only by `AuthoringController`; `delivery-schemas.ts` reaches nothing from that family, and the delivery controller imports only it. The shared `runOperation` carries no DTO of either family, so sharing it does not make an `Authoring*` shape reachable from delivery. **The request schemas are the generated ones, imported not restated** — the controllers add only the composition of a path parameter with a body, which is where a hand-rolled object would otherwise get a field name wrong. `depth` on the solution route is validated, never defaulted: `basic` is an unconditional grant and `full` is entitlement-gated, so a request meaning neither is refused rather than quietly served the cheaper one. Problem Details carries `location` and structured `errors`, so a refused publication hands the validation panel the precondition list to group by code rather than a message to string-match, and `Entitlement` maps to **402, not 403** — "upgrade to access" and "you are not permitted" need different UX and different metrics (§8). M3-34's own criterion is asserted the way it is worded: the key-absence scan runs over **live controller output** from all three delivery routes, with the same scan finding `correctOptionId` on an authoring response as the non-vacuity control. F36 proven with a planted policy-less handler that fails module construction. |
| M3-35 | 2026-08-12 | 37 | `packages/content-renderer/` — the one `ContentRenderer` (F20), asserted by a monorepo-wide scan that also proves it scanned >50 files, since an empty scan finds one implementation just as well as a correct one. **The surface is a parameter, not four components**, and a test strips the surface marker and asserts the four renderings are byte-identical — which is what makes M3-38's parity claim about something. **An unknown node renders a visible labelled fallback and reports it**: never throws, because a renderer that throws takes a whole paper down over one node, and never renders nothing, because a silently dropped node is an item asking about a figure the student cannot see. The fallback's element follows its position — a `span` inline, a `div` in block flow — after jsdom caught the first version emitting a `div` inside a `p`, which closes the paragraph and puts the rest of the sentence outside it: a degradation that breaks the page it exists to save. The authored `textAlternative` is the accessible name on every notation node, with the source `aria-hidden` for M3-36 to replace. Media renders as a **reference** with a host-supplied resolver, and an unresolved asset still renders its description — ACC-03's guarantee is the description, not the picture. **The node vocabulary is declared in the package** because a package cannot import an app and `domain/` imports nothing, so `renderer-seam.spec.ts` in `apps/api` asserts both sides agree on the block kinds, inline kinds, marks, size hints *and schema version* — the same instrument as the M2→M3 key seam, for the same reason: otherwise a new kind ships as a fallback box and nobody finds out until a mark is disputed. F24 asserted by source scan with the token module the only exemption; F19 by scan for any import of an app. |
| M3-36 | 2026-08-12 | 30 | Mathematical notation → **real MathML** — `<mfrac>`, `<msup>`, `<msqrt>`, `<msubsup>` — never HTML-and-CSS that looks like mathematics, which is the whole reason TECH-STACK chose a MathML emitter over KaTeX. **TECH-STACK names Temml and Temml is not in the dependency tree**: no network here, and leaving the render path unbuilt for a milestone is not an option when M3-11's publication precondition consumes a render verdict. So the subset JEE/NEET items actually use is emitted by an in-repo converter behind one module, and swapping Temml in is a change to `latex-to-mathml.ts` and nothing else. Recorded as debt **D26** with a named trigger: the first authored expression the subset cannot express. The converter is a pure function — no identifiers, no counters, no clock — so determinism is structural rather than tested-into-existence; asserted anyway over 100 calls and across all four surface profiles. **The authored `textAlternative` is the accessible name and the MathML is `aria-hidden`**, so a screen reader hears one reading rather than two competing ones: `\frac{1}{2}mv^2` read structurally is "fraction one over two", and the author wrote "one half m v squared". An unknown command is **refused, not passed through** — a command nobody taught the renderer reaches a student as literal backslash text — and invalid LaTeX renders the alternative with a visible affordance and reports `NOTATION_UNRENDERABLE` through the renderer's own issue channel, which is what FR-QM-14 rule 2 blocks publication on. Digits run together into one `<mn>`, so 9.81 is a number and not four. Two spec defects found and fixed: a JSX attribute string does not process escapes, so `latex="\\frac"` was feeding the converter a literal double backslash. |
| M3-37 | 2026-08-12 | 32 | Chemical notation through the **same** MathML pipeline as mathematics, so a screen reader meets one kind of markup and the parity check has one kind of output to compare. Formulae, multi-letter symbols, charges, states, single and equilibrium arrows, stoichiometric coefficients, hydrate dots and bracketed groups. **The ambiguity worth naming: `H2O` and `Ca2+` write digits in the same place and mean opposite things** — a subscript in one, a charge magnitude in the other. What disambiguates is whether a sign follows, so the lookahead decides, and a formula needing both is written explicitly as `SO4^2-` (braced form accepted too). Getting this wrong renders calcium-two rather than a calcium ion, which is a chemistry error on a student's screen, so it has its own test. **A structure degrades to the documented affordance, not to a broken picture** (DEC-6, D19): `\chemfig`, SMILES ring closures and bond-angle syntax are refused with `STRUCTURE_NEEDS_DIAGRAM_ASSET` and a message naming the diagram route, and the two refusals are deliberately different codes — malformed notation is a defect the author fixes, a structure is content in the wrong medium, and only one of those is answered by "use a diagram asset". A half-drawn ring would tell the author nothing. Deterministic by construction and asserted over 100 calls and all four surface profiles; axe clean rendered and degraded alike. The generic `Notation` component from M3-35 is gone — both notation classes now have their own node, and leaving a third path that rendered raw source would have been the divergence F20 exists to prevent. |
| M3-38 | 2026-08-12 | 25 | Render validation across four surfaces. **Parity is structural, not tested into existence**: the Studio preview and the delivery render are the *same call* — one component, one surface parameter, one `renderFor` entry point — so "byte-for-byte identical" is a property of there being one code path. A function that rendered twice and compared would have been a tautology dressed as a check, so `serializationsAgree` takes both sides and the spec hands it a **planted divergence** first: the instrument is shown to fail before it is trusted to pass. The fixture corpus covers every node kind, asserted by walking the fixtures and differencing against `BLOCK_KINDS`/`INLINE_KINDS` rather than counted by hand. Verdicts name the surface **and** the block index, because "it does not render" is the same unusable message "invalid item" is. **An unresolved figure deliberately does not block**: it is a host condition, not a document defect — the same document renders once the asset resolves, and blocking would refuse to publish an item whose asset simply was not handed to a validator. A chemical structure *does* block, which is DEC-6 reaching publication rather than stopping at the editor. **`RenderValidator` still has no production adapter** — calling `validateRender` from the API means running a React render inside the Node service and there is no composition root (no running application, M0), so the precondition is today enforced against a fact only a test supplies. Recorded as debt **D27** at the port itself, where the next reader meets it. |
| M3-39 | 2026-08-12 | 27 | Studio shell, sidebar and the 1280 px gate (DEC-5, debt D3). **The destination a caller is on is a prop and changing it is a callback**, so the shell is a pure function of its inputs and there is no router, no `main.tsx` and no dev server to stand up — DEC-5's whole point, and asserted rather than remembered: the spec scans the three modules for a router import and asserts `src/main.tsx`, `index.html` and `vite.config.ts` all do not exist, so "M3 did not borrow M0's work" is a failing test the day somebody does. **Every destination is listed and the unbuilt ones are `enabled: false` with a stated reason**, not absent — a navigation model that grows by enabling produces the diff "Review Queue is live now", one that grows by appearing makes a missing destination indistinguishable from a forgotten one. Disabled destinations stay in the tab order and carry the reason as an accessible description, because a control that vanishes from the keyboard is one a keyboard user cannot discover exists; `aria-disabled` rather than `disabled` is what buys that. The gate renders **no authoring surface at all** below the floor rather than squeezing it — an answer key on a phone screen on a train is the failure being prevented — and it says how wide the window actually is, since "too small" is the same unusable message "invalid item" is. Two spec defects fixed here: the keyboard traversal started from the `<h1>` the shell had just focused, which sits after the sidebar in DOM order so `tab()` walked forward past everything (focus is released first, with the reason recorded); and the source scan used `new URL(…, import.meta.url)`, which **Vite rewrites to the path it serves**, so `readFileSync` was handed `/src/shell/StudioShell.tsx` — it reads from the working directory now, and asserts it scanned three files so an empty scan cannot pass. |
| M3-40 | 2026-08-12 | 50 | **Item Editor** — one of the four pages that carry the product (FRONTEND §3). **The preview is not *like* the delivery render, it is the same call**: `ContentRenderer` with a surface parameter, defaulting to the minimum device profile (FR-QM-14 rule 3), and the spec asserts the preview DOM equals `renderFor(body, 'mobile')` — the delivery entry point — with a planted divergence shown to fail first. The one wrinkle is a spelling difference, not a rendering one: `renderToStaticMarkup` writes `style="color:x"` and React's client render reads back `color: x;` from the CSSOM, so the delivery side is re-normalized through the same CSSOM rather than the assertion being loosened to `toContain`. **Dual-mode notation is lossless because there is one representation**: the LaTeX string, with the palette an insertion device over it rather than a second model to fall out of step — and the palette's *reading* of an expression keeps text it does not recognise as a literal segment, with `join(segment(x)) === x` over six expressions and a deliberately lossy segmenter shown to break it. **The editor decides no governance rule**: findings arrive from the domain (M3-17) through the port and `maySubmit` is read, never recomputed, asserted by a source scan finding none of the ten domain finding codes in the feature. What it adds is *form errors*, named for the field rather than the rule, because a blank stem should not need a round trip to discover — client validation is a courtesy and the server refuses the same drafts again (FRONTEND §7). Autosave is `UpdateItemDraft` with an idempotency key, debounced, and **a keystroke landing while a save is in flight marks the draft dirty so the completing save starts another with the newer snapshot** — proven by holding the first save open, typing into it, and asserting the second save carries both characters. A failed autosave says so out loud: a quiet "saved" is how forty minutes of equation authoring is lost. The answer key is edited here and nowhere else, asserted by scanning every Studio source file for four key-bearing names and finding them only under `features/item-editor/`, with the editor itself as the non-vacuity control. Distractor prompts ask for the **misconception** per incorrect option and route it to `UpdateSolutionDraft`'s analyses — prompted where the author still has the item in their head, stored where a distractor analysis belongs. Added `@questionbank/content-renderer` to Studio's dependencies (workspace link, no network). |
| M3-41 | 2026-08-12 | 42 | Stimulus and Solution editors. **Attach-existing comes before create-new, and the ordering is asserted by document position** — an editor that offers "new passage" first gets a new passage every time, and five copies of one passage diverge the moment one is corrected, which DOMAIN-MODEL calls the category's canonical fatal error. The reference list is shown **before** the attach button, also asserted by position: "used by four items" is a fact an author can act on beforehand and worth nothing afterwards. Attachment reports the **pinned version number** in the confirmation, because FR-TCH-03 rule 2's guarantee is invisible otherwise and "the passage changed under my item" is the surprise it exists to prevent. The solution editor puts the item and its key alongside the derivation, marks which option *is* the key, and **surfaces final-answer disagreement the moment autosave runs** — no submit, because the learner who reads a derivation ending in 9.8, answers 9.8 and is marked wrong is entirely right to dispute it. The verdict is the domain's: a spec scans the feature for `tolerance` and `parseFloat` and finds neither, so nothing here forms a second opinion about what the executor would mark correct. Step **ordinals are recomputed from position on every save, never authored** — the repositories' rule for projections, for the same reason — and reordering is buttons rather than a drag handle, proven operable from the keyboard alone with the ends disabled rather than absent. Distractor analysis is prompted per incorrect option **with the option's own body rendered beside it**, since an author asked about "option 3" in the abstract writes about the wrong one. **Three shared modules extracted rather than copied**: `authoring/body-draft.ts` (the draft that maps onto the DEC-2 vocabulary), `authoring/BodyEditor.tsx` (the one notation field, dual-mode) and `authoring/use-autosave.ts` (the debounce, and the in-flight keystroke that must not be lost). Three copies of a notation field would be three places for the mode switch to drop an expression and only one would be found before an author did. The answer-key scan moved with them into `authoring/key-boundary.spec.ts` and became **ADR-0009's shape at the Studio level**: an enumerated, closed list of features allowed to carry a key, asserted in both directions, with the scanner file itself the one named exemption. |
| M3-42 | 2026-08-12 | 19 | Media library. **Alt text is a precondition of the register action, not a validation after it** — the button is unavailable until it is there, and the waiting list says what is missing with a link to the field. The same argument M3-15 makes in the constructor, at the surface that produces the content: an asset that can be registered and fixed later is one that gets referenced before anybody fixes it, then either published under deadline or corrected by whoever happens to notice. A `diagram`, `chart`, `graph` or `reaction_scheme` additionally needs a long description, because "a graph of velocity against time" tells a screen-reader user that a figure exists and nothing it shows. **The asset-type vocabulary is the generated one** — `AssetTypeSchema` from `@questionbank/contracts/content-schemas`, derived from the OpenAPI document — so the browser cannot drift from the domain, and a test asserts the *complement* of the information-bearing list is exactly `photograph`, meaning a type added to the document lands here unclassified and fails rather than defaulting to "needs no description". **No MIME allowlist exists in the browser at all**: bytes go to the store at the upload edge and the store answers with what it holds (M3-27), so the form records the answer instead of asking the author to declare the thing the domain refuses to trust. Retirement is **not disabled silently** — the standing note says how many published items reference the asset, the button stays available, and the domain's own refusal is what the author reads; an author who cannot act and cannot see why concludes the tool is broken. **Fixed a genuine flake this task exposed rather than re-running it green**: M1's `TaxonomyBrowser` performance test asserted a 200 ms wall clock in a jsdom worker, so it measured machine contention and failed the day the suite grew to eleven files. It now compares four times the nodes against a baseline — a ratio survives contention — and asserts expansion is linear rather than quadratic (measured 2.97× against a threshold of 8; quadratic would be 16). PER-01's real budget is a browser measurement and stays **D10**. |
| M3-43 | 2026-08-12 | 23 | Item browser and the validation panel. **Filters live in the URL** (FRONTEND §5) — a reviewer sending "the eleven physics items awaiting changes" sends a link, not instructions for reproducing a screen — and they are **typed and validated rather than parsed ad hoc** (§8): a hand-edited `?state=marinated` is dropped here rather than forwarded, because a filter nobody can name is a query whose result nobody can explain. There is still no router (DEC-5), so the query string is reached through a two-method port; the spec exercises both the fake **and** the real `history.replaceState` implementation, so the half a router eventually replaces is not the untested half. `replaceState`, not `pushState` — typing into a subject filter must not fill the back button. The round trip is asserted **over every lifecycle state**, so none is quietly unfilterable. **Asking for drafts locks the author filter to the person asking**, with the reason stated and bound by `aria-describedby`: FR-TCH-06 rule 1 is the server's to enforce (M3-29 answers a learner with `Authorization`), and what the surface adds is that nobody discovers the rule by being refused — the assertion is on the *query sent*, not on filtering rows the fake handed back, which would have tested the fake. Two **designed** empty states rather than one defaulted one (UX §12): a filter that matched nothing offers a way out, an empty corpus says what will fill it, and the rule is that an empty state offers an action or explains — never both blank and silent. A published row names its version number and links to the history with diffs (FR-QM-02 rule 4); a draft row shows neither, asserted. The panel groups blocking and warning separately with each finding's location, and **`describeDuplicateCheck` is asserted to contain "has not run" and to contain no "none"** — a report claiming no duplicates when nothing ran is a claim a reviewer acts on. `LIFECYCLE_STATES` comes from the generated `LifecycleStateSchema`, so the filter list cannot drift from the document. **Track E complete.** |
| M3-44 | 2026-08-12 | 41 | Fitness functions and coverage thresholds. `content-rules.ts` holds the judgment as **pure functions over their inputs**, so every gate runs against the real tree *and* against a planted violation committed under `fitness-fixtures/` — a gate nobody has seen fail is a gate nobody knows works. **F6/F35 is asked differently in each direction, and each asymmetry has a reason**: a delivery surface must not *name* a key field in its own text, because naming it is how it gets serialized and following its imports would flag the domain's own `ResponseSpecification`, which every layer legitimately reaches; an authoring surface must *reach* key material, because it carries the key by handing on a whole specification and by importing the **generated** Zod schema rather than restating field names — a text scan of the module alone reported the correct design as a violation, which is how the asymmetry was found. **ADR-0009 condition 3 is now the import graph M3-33 deferred**: the walk from `content.controller.ts` reaches 17 modules, three hops deep, and the check is proven by asking the same question of `authoring.controller.ts`, which does reach the family. A rename does not defeat it, because the graph is over paths. **INV-01's structural half is a tripwire**: `contexts/ai/` and `contexts/generation/` do not exist yet, so the production patterns have nothing to find — stated plainly, with the mechanism proven against a context that does. Its behavioural half is asserted directly against `checkNoMachinePublishesItsOwnContent` with real AI provenance. **F20 moved here from the renderer package**: two copies of the check would have been the same mistake the rule is about, and the planted second implementation lives under `apps/`, so the production scan names its one exemption. The scan also grew a `.tsx`-aware walker — `tsFilesUnder` sees only `.ts`, so the old scan would have reported exactly one implementation *by being unable to find any*. The ADR-0008 list **polices itself three ways** (missing threshold, threshold below 100, threshold naming a deleted module) and, beyond the spec's brief, **classifies every content module**: 38 correctness-bearing and 17 not, so a new module cannot arrive unclassified. F5 and F7/F40 run over real catalogue rows with the judgment shared with the unit spec; the TRUNCATE check names `questionbank_app` and **records that the role does not exist locally** rather than treating whatever grantee it found as the app role, which would have called the table owner a violation. Studio gained coverage thresholds — 80/70 overall, **no 100% module, and that is ADR-0008's rule rather than an omission**: the Studio settles no question, which is why the item editor reads `maySubmit` instead of recomputing it. Fixed a real defect found here: Studio's `@vitest/coverage-v8` was 4.1.10 against vitest 3.2.7 and the provider **could not load at all**, so `--coverage` had never run; pinned to 3.2.7 from the store and added `--coverage` to the test script. Studio measures 97.10% line / 92.09% branch, gate verified failing at 99 first. apps/api 91.42% line / 97.44% branch, every threshold met. Golden set: 40 pass, 0 official papers, 4 synthetic — **B1 still open**. |
| M3-45 | 2026-08-12 | 21 | Import corpus — 500 records, **generated deterministically and committed**, with the spec regenerating it and comparing byte for byte so the artifact and its builder cannot drift; a hand-written 500-line file is unreviewable and rots the first time a field is renamed. No clock and no randomness: variety is a 31-bit linear congruential step over the record index, so the rejection set is a fact rather than a distribution. **"A correct rejection report" is read as the set matching** — a `Map` of line number to code, compared exactly — and the instrument is shown to fail in *both* directions before it is trusted, by repairing a planted defect and by spoiling an accepted record, both edited **by line position rather than by text search**, since a replacement that silently matched nothing would leave the corpus intact and pass by having changed nothing. **The corpus found two real defects, and they are fixed rather than expected.** (1) A `MATH_BLOCK` with a blank `textAlternative` **imported cleanly**: the parser fabricates a `ContentBody` from JSON by assertion and nothing ever ran `createContentBody`, so import could create a draft the editor refuses — the one thing DEC-7 says it must not do — and put an equation with no reading order into the corpus, breaking ACC-02. Every authored body, stem and option alike, now goes back through the domain constructor. (2) A numeric spec missing its tolerance was refused **by a database CHECK constraint**, reaching the report as `PERSISTENCE_REJECTED` carrying a raw Postgres message — SQL in a client payload (§8), and a message that tells an author a constraint name instead of that their item has no tolerance. `checkSpecificationIsScorable` now guards every write path, interactive and import, which is DEC-3's "a specification whose projection the executor refuses cannot be saved" actually holding rather than being described. **Three of the nine rejection classes M3-45 names are not import rejections, and the corpus says so rather than the check being narrowed until they are**: `unresolved` licensing is the *default* a draft starts from (M3-06) and FR-QM-05 rule 4 blocks publication, not import; a solution is a publication precondition, and requiring one would make a previous-year corpus unimportable, which is the corpus this path exists for; and "unrenderable" is `validateRender`'s verdict at publication, so a second weaker notion here would be the parallel validator DEC-7 forbids. All three are carried as **accepted** records and asserted to import, with the reasoning in the spec. 488 imported, 12 rejected, every accepted record retrievable as a draft. Golden set: 40 pass, 0 official papers, 4 synthetic — **B1 still open**. **Track F complete.** |
