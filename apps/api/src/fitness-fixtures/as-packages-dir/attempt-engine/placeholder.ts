// A fixture standing in for a real packages/attempt-engine/ that appeared
// without frontend-rules.ts's ATTEMPT_ENGINE_PACKAGES list being updated —
// see frontend-rules.spec.ts's "unconfirmed package" test.
export {};
