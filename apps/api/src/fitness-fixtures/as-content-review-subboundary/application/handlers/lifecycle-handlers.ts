/**
 * Stands in for the real `application/handlers/lifecycle-handlers.ts` —
 * authoring-specific application plumbing, not one of the three permitted
 * targets.
 */
export const fakeRecordItemReviewDecisionHandler = 'lifecycle-handlers';
