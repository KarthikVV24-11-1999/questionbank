/**
 * Stands in for authoring-side application plumbing — a handler, a query, a
 * command. Neither a domain module nor `application/authorization.ts`, so
 * the review plumbing importing this is the violation
 * `planted-review-reaches-authoring.ts` plants.
 */
export const fakeHandler = 'handler';
