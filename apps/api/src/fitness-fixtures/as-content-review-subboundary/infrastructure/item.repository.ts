/**
 * Stands in for `infrastructure/item.repository.ts` — an ordinary
 * authoring-side infrastructure module, not one of the four shared-contract
 * exceptions. Review plumbing reaching it must still fail.
 */
export const fakeItemRepository = 'item.repository';
