/**
 * Not production code. Stands in for the barrel — the fixture-tree
 * equivalent of `contexts/content/public/index.ts` (M4-35) — so a planted
 * violation can prove the gate catches a module reaching its own barrel
 * rather than the review modules directly.
 */
export const barrelValue = 'barrel';
